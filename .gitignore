# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Virtual environment
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# Large files (Phase 2 checkpoints are huge due to generator)
# Note: we sometimes create variants like checkpoints_cwq_phase2_paperprompt/
checkpoints_*_phase2/
checkpoints_*_phase2*/
*.gguf

# Phase 1 checkpoints and heuristics (stored on Hugging Face)
# Downloaded automatically by setup_environment.sh from:
# https://huggingface.co/rhordoan/drag-improved-checkpoints
checkpoints_cwq_subgraph/phase1_best.pt
checkpoints_webqsp_subgraph/phase1_best.pt
data/train_heuristics_cwq.jsonl
data/train_heuristics_cwq_train.jsonl
data/train_heuristics_cwq_val.jsonl
data/train_heuristics_webqsp_subgraph.jsonl

# Keep intermediate epoch checkpoints (smaller, useful for debugging)
!checkpoints_cwq_subgraph/phase1_epoch_*.pt
!checkpoints_webqsp_subgraph/phase1_epoch_*.pt

# Temporary files
*.log
*.tmp
/tmp/

# Cache
.cache/
*.cache
unsloth_compiled_cache/

# OS
.DS_Store
Thumbs.db

# Large data files (can be regenerated)
data/kg/
data/*.zip
*.tar.gz

# RoLit-KG pipeline outputs
data/processed/
data/*_full.jsonl
outputs/
logs/

# Default: don't commit datasets; keep only tiny samples
data/*
!data/rolit_kg_sample/
!data/rolit_kg_sample/*.jsonl

# LaTeX build artifacts (we keep the .tex/.pdf)
docs/*.aux
docs/*.bbl
docs/*.out
docs/*.toc
